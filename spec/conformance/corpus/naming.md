---
title: Naming conventions
url: https://conformance.test/naming
---
# Naming conventions

## Snake case

The `foo_bar` symbol uses snake case. Prefer `foo_bar` over camel case in this
codebase. The `package.module` path notation is dot separated.

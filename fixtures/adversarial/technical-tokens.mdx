---
title: Technical token torture test
url: https://vendor.example/docs/tokens
---
# Exact symbols

The index must preserve `std::move`, `useEffect`, `foo_bar`, `package.module`, `AP-104`, `v4.2.1`, and `auth/key.json` as useful technical tokens.

## Unicode

Search should preserve café, naïve, Straße, 東京, and identifiers surrounding non-ASCII prose.

## Embedded markup

<Callout type="warning">
This conservative MDX element must not crash ingestion or execute anything.
</Callout>

```rust
fn move_value<T>(value: T) -> T {
    value
}
```

